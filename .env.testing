# Banco de teste (QA-03). Sobe com: docker compose -f docker-compose.test.yml up -d
# NUNCA aponte para produção — os testes truncam tabelas.
APP_ENV=testing
APP_KEY=test-key-32-chars-long-padding00
APP_URL=http://localhost:8000
DB_ADAPTER=pgsql
DB_HOST=127.0.0.1
DB_PORT=55432
DB_NAME=yve_test
DB_USER=postgres
DB_PASS=test
QUEUE_SECRET=test-secret

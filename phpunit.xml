<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         stopOnFailure="false">

    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>

    <!-- O banco é decidido no tests/bootstrap.php: .env.testing (Postgres em
         Docker) quando existir, senão SQLite em memória e os testes de feature
         se auto-skipam. Não fixe DB_* aqui — sobrescreveria o .env.testing. -->
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_KEY" value="test-key-32-chars-long-padding00"/>
    </php>
</phpunit>
